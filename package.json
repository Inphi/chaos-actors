{
  "private": true,
  "name": "chaos-actors",
  "version": "0.0.1",
  "description": "Actors for testing the Optimism chaos devnet",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist/*"
  ],
  "scripts": {
    "start": "ts-node ./src/service.ts",
    "build": "tsc -p ./tsconfig.json"
  },
  "dependencies": {
    "@eth-optimism/sdk": "^2.0.0",
    "@eth-optimism/contracts-bedrock": "^0.13.0",
    "@eth-optimism/common-ts": "0.8.0",
    "ethers": "^5.7.0"
  },
  "devDependencies": {
    "ts-node": "10.9.1",
    "@ethersproject/abstract-provider": "^5.7.0",
    "typescript": "^4.9.3"
  }
}
